version: "3.8"
services:
  api:
    build: ./apps/api
    ports:
      - "3000:3000"
  web:
    build: ./apps/web
    depends_on: [api]
    ports:
      - "8080:80"
    environment:
      - VITE_API_URL=http://localhost:3000
